# Google OAuth client ID for Drive backup, drive.file scope only.
# The GCP project must be in Production publishing status. In Testing status
# refresh tokens expire after 7 days and backup stops silently.
# Backup stays inert while this is empty, and everything else keeps working.
VITE_GOOGLE_CLIENT_ID=
